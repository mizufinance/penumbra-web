# Deprecated Shieldd UI components

This `components` directory contains deprecated Shieldd UI components. We are in the process of building new versions of Shieldd components; they will live in the `src` directory of this package. Only use a component from this `components` directory if a newer version is not yet implemented in the `src` directory.
